rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Tenant-isolated school configuration and directories
    match /schools/{schoolId} {
      allow read, write: if true;
    }

    match /schools/{schoolId}/{document=**} {
      allow read, write: if true;
    }
  }
}
